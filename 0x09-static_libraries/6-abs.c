#include "main.h"

/**
 * _abs - checksout the absolute number
 * @n: checked integer
 * Return: the absolute value is returned
 */
int _abs(int n)
{
	/*checks for absolute numbers*/
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

